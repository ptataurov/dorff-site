mixin btn({ name, href }, mixes)

  - const data = require('views/components/btn/data.js')
  - const css = require('views/components/btn/_css.json')

  if (href)
    a(class=css.btn class=mixes href=href)= name
  else
    button(class=css.btn class=mixes)= name
